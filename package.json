{
  "name": "delhi-crime-dashboard",
  "version": "1.0.0",
  "description": "Delhi district crime, road-safety, and infrastructure coverage dashboard",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:liquor-crash": "node build_liquor_crash_proximity.js && node build_liquor_crash_analysis.js",
    "build:ward-infra": "node build_ward_infra.js",
    "build:interactive-map": "node build_ward_infra.js && node build_interactive_map.js",
    "test": "node --test test/liquor_crash_proximity.test.js test/exploratory_score.test.js"
  },
  "dependencies": {
    "@turf/turf": "^7.3.5"
  }
}
